# Kadr

Бесплатная программа для скриншотов и записи экрана под Windows 10 (сборка 19041 и новее) и Windows 11.
Аналог «Скриншотера» без облака,для личного использования. 

## Возможности

- **Скриншоты**: область (`PrtScr`), активное окно (`Alt+PrtScr`), активный монитор (`Shift+PrtScr`), все мониторы (`Ctrl+PrtScr`).
- **Редактор поверх экрана**: стрелка, линия, карандаш, маркер, прямоугольник, овал, текст, числовая метка, размытие;
  выделение и перемещение объектов, ресайз за ручки, undo/redo, палитра и свой цвет, толщина колесом мыши, тени.
  Действия: копировать (`Ctrl+C`), сохранить (`Ctrl+S`), печать (`Ctrl+P`), распознать текст (OCR Windows), готово (`Enter`), закрыть (`Esc`).
- **Скриншот с прокруткой**: автопрокрутка колесом, склейка кадров детектором сдвига, превью, автостоп по концу страницы.
- **Запись видео** (`Ctrl+Shift+PrtScr`, пауза `Shift+Pause`): MP4 H.264 + AAC через Windows.Graphics.Capture и Media Foundation,
  системный звук и микрофон, пресеты качества до 4K, подсветка курсора и кликов, веб-камера в кадре, контроль места на диске,
  панель управления, отдельный процесс видеомодуля.
- **Трей**: меню, панель быстрого выбора, тихий режим, автозапуск, вызов двумя кнопками мыши.
- **Настройки**: `%LocalAppData%\Kadr\settings.json`, шаблоны имён файлов, папки, формат PNG/JPEG, устройства звука и камера.

## Системные требования

**Скриншоты и редактор**

- Windows 10 версии 2004 (сборка 19041) и новее или Windows 11, 64-бит.
- Память 100 МБ, процессор любой, права администратора не нужны.
- Для распознавания текста: языковой пакет Windows с нужным языком (интернет не нужен).

**Запись видео**

- То же плюс видеокарта с поддержкой Direct3D 11 (любая современная, включая встроенную).
- Для 1080p 60 к/с и 4K: аппаратный кодер H.264 (Intel Quick Sync, NVIDIA NVENC, AMD VCE/VCN); без него эти режимы
  сильно грузят процессор, 720p и 1080p 30 к/с работают и на программном кодере.
- Микрофон и веб-камера.

## Структура проекта

- `src/Kadr.Common` — настройки, хоткеи, шаблон имени файла, протокол IPC.
- `src/Kadr.Capture` — мониторы, DPI, захват экрана через GDI.
- `src/Kadr.Recording` — библиотека записи: WGC + Direct3D, WASAPI, MediaStreamSource + MediaTranscoder, подсветка курсора (Direct2D).
- `src/Kadr.Recorder` — процесс видеомодуля `Kadr.Recorder.exe` (протокол по named pipe, режим `--test`).
- `src/Kadr.App` — WPF-приложение: трей, хоткеи, оверлей и редактор, прокрутка, запись, настройки.
- `assets/icons` — иконки и генератор.
- `tests/` — скрипты автотестов и тестовая страница для прокрутки.
- `publish/` — готовая сборка.


## Сборка и запуск

```bash
dotnet build D:\Work\Kadr\code\Kadr.sln -c Release
```

Запуск: `src\Kadr.App\bin\Release\net8.0-windows10.0.22621.0\Kadr.exe`. Нужен .NET 8 Desktop Runtime.

Готовая сборка без зависимости от установленного .NET:

```bash
dotnet publish D:\Work\Kadr\code\src\Kadr.App\Kadr.App.csproj -c Release -r win-x64 --self-contained true -o D:\Work\Kadr\code\publish
```

Аргументы: `-r` скриншот области, `-w` активное окно, `-m` активный монитор, `-d` все мониторы,
`-v` запись видео, `-c` скриншот с прокруткой, `-u` закрыть работающий экземпляр,
`-s` тихий запуск без окна настроек, `--portable` хранить настройки рядом с exe (или создать файл `portable` рядом с exe).

Логи: `%LocalAppData%\Kadr\log\main-*.log` (приложение) и `video-*.log` (видеомодуль).

## Тестовые скрипты (`tests/`)

- `test-record.ps1` — запись 5 секунд через `Kadr.Recorder --test` без приложения (`-Mic`, `-Sys`, `-Highlight`).
- `test-video.ps1`, `test-scroll.ps1`, `test-pause.ps1`, `test-more.ps1` — автотесты через приложение (двигают мышь и клавиатуру).
- `grab-frame.ps1` — извлечь кадр из MP4 в PNG.

## Лицензия

Автор: Mike Alimov. Код Kadr распространяется по лицензии
[PolyForm Noncommercial 1.0.0](LICENSE): использовать, менять и передавать программу можно только в
некоммерческих целях (личное использование, учёба, хобби, некоммерческие организации). Коммерческое
использование, продажа и встраивание в платные продукты без отдельного разрешения автора запрещены.
При передаче копий нужно сохранять текст лицензии и строку «Required Notice: Copyright Mike Alimov».
Гарантий нет, программа предоставляется «как есть».

Сторонние библиотеки и их лицензии перечислены в [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md); все они
разрешительные (MIT и BSD-3-Clause) и не накладывают ограничений на лицензию Kadr. Кодеки, OCR и захват экрана
берутся из самой Windows и в дистрибутив не входят.

Kadr — самостоятельная программа.


