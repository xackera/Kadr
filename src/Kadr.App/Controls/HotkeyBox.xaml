<UserControl x:Class="Kadr.App.Controls.HotkeyBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True" Cursor="IBeam" Width="280" Height="34">
    <Border x:Name="Root" CornerRadius="6" BorderThickness="1" Background="#F7F7F7" BorderBrush="{StaticResource BorderBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="Text" Margin="12,0" VerticalAlignment="Center" FontSize="14" Foreground="{StaticResource TextBrush}" />
            <Button x:Name="ClearButton" Grid.Column="1" Width="26" Height="26" Margin="0,0,4,0" Cursor="Hand"
                    Background="Transparent" BorderThickness="0" ToolTip="Очистить" Focusable="False" Click="Clear_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <Ellipse x:Name="E" Width="16" Height="16" Fill="#8A8A8A" />
                            <TextBlock Text="✕" FontSize="9" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="E" Property="Fill" Value="#FF3B30" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>
