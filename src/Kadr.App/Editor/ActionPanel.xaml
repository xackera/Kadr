<UserControl x:Class="Kadr.App.Editor.ActionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="False">
    <UserControl.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="0,1" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="Foreground" Value="#303030" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="B" Background="Transparent" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="Background" Value="{StaticResource AccentLightBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="White" CornerRadius="8" Padding="4,6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.3" />
        </Border.Effect>
        <StackPanel>
            <Button Style="{StaticResource ActionButton}" Content="&#xE8D2;" ToolTip="Распознать текст" Click="Ocr_Click" />
            <Rectangle Height="1" Margin="4,4" Fill="#E0E0E0" />
            <Button Style="{StaticResource ActionButton}" Content="&#xE8C8;" ToolTip="Копировать в буфер обмена (Ctrl+C, Ctrl+Insert)" Click="Copy_Click" />
            <Button Style="{StaticResource ActionButton}" Content="&#xE749;" ToolTip="Распечатать на принтере (Ctrl+P)" Click="Print_Click" />
            <Button Style="{StaticResource ActionButton}" Content="&#xE74E;" ToolTip="Сохранить на диск (Ctrl+S)" Click="Save_Click" />
            <Button x:Name="BtnDefault" Style="{StaticResource ActionButton}" Content="&#xE73E;" ToolTip="Готово: сохранить по настройкам (Enter)" Click="Default_Click" />
            <Button Style="{StaticResource ActionButton}" Content="&#xE8BB;" ToolTip="Закрыть (Esc)" Click="Close_Click" />
        </StackPanel>
    </Border>
</UserControl>
